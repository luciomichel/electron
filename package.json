{
  "name": "app-electron",
  "version": "1.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --mac --win --publish never",
    "deploy": "electron-builder build --mac --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^21.2.0",
    "electron-builder": "^23.6.0"
  },"repository": {
    "type" : "git",
    "url" : "https://github.com/luciomichel/electron.git"
  },
  "dependencies": {
    "electron-updater": "^5.3.0"
  }
}
